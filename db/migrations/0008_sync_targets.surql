-- =============================================================================
-- 0008_sync_targets — registered folder-sync targets (nopal sync)
-- =============================================================================

DEFINE TABLE IF NOT EXISTS sync_targets TYPE ANY SCHEMALESS PERMISSIONS NONE;
