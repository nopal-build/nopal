-- =============================================================================
-- 0005_relationships — human-to-human relationships (vault sharing visibility)
-- =============================================================================

DEFINE TABLE IF NOT EXISTS relationships TYPE ANY SCHEMALESS PERMISSIONS NONE;
