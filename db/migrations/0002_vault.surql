-- =============================================================================
-- 0002_vault — file refs and vault folders
-- =============================================================================

DEFINE TABLE IF NOT EXISTS file_refs    TYPE ANY SCHEMALESS PERMISSIONS NONE;
DEFINE TABLE IF NOT EXISTS vault_folders TYPE ANY SCHEMALESS PERMISSIONS NONE;
